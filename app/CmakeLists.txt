cmake_minimum_required(VERSION 3.5)

file(GLOB_RECURSE SRCS *.[ch]pp)

add_executable(app ${SRCS})

target_link_libraries(app PRIVATE ${CERES_LIBRARIES}
Eigen3::Eigen ${OpenCV_LIBS} comp_vis_lib)